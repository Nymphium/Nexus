fn fib(n: i64) -> i64 do
  if n == 0 then
    return 0
  endif
  if n == 1 then
    return 1
  endif
  return fib(n: n - 1) + fib(n: n - 2)
endfn

fn main() -> unit effect { IO } do
  let result = fib(n: 30)
  perform print(val: [=[Fib(30) = ]=] ++ i64_to_string(val: result))
  return ()
endfn
